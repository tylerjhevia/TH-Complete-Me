class CompleteMe{
  constructor() {
    this.root = null;
  }

  createNode(word) {
    if(!root) {
      this.root = new Node(word);
      return
    }
    let currentNode = root;
    let newNode = new Node(word);
  }
}
